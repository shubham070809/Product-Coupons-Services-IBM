eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
management:
  endpoints:
    web:
      exposure:
        include: '*'
server:
  port: 8888
spring:
  application:
    name: api-gateway-ws




  cloud:
    gateway:
      discovery:
        locator:
          enabled: true

